clc
clear
close all

% a
t= 0:0.1:20;
x=t.*exp(-0.1*t).*cos(t);
plot(t,x)
title('orignal')


%extra
figure

plot(-t,x)
title('flip')

% b  even = (x(t)+x(-t))/2
flip

% plot (t,x)   VS  plot (x)
%length(x)
% matalb me 0 index ni hota
